// This is a comment and will be ignored when the program runs.

// Setup variables
var canvas = document.getElementById('canvas');
var context = canvas.getContext('2d');

// Choose color
context.fillStyle = "red";
// Draw Rectangle - context.fillRect(x, y, width, height)
context.fillRect(0, 0, 100, 100);